import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mynuaxess.www',
  appName: 'NuAxess Employer',
  webDir: 'dist/fuse',
  bundledWebRuntime: false
};

export default config;
